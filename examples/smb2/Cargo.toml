[package]
name = "smb2-example"
version = "0.1.0"
edition = "2021"

# Targets smb2 0.11.x — https://crates.io/crates/smb2
[dependencies]
smb2 = "0.11"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
